package by.freddykray.AI.Voice.Organizer.llm;

import org.springframework.stereotype.Component;

import java.time.OffsetDateTime;
import java.time.ZoneId;
import java.util.Map;

@Component
public class PromptRegistry {

    private static final ZoneId APP_ZONE = ZoneId.of("Europe/Moscow");

    private final Map<PromptType, String> staticPrompts = Map.of(
            PromptType.DETECT_TYPE, """
                    Ты определяешь тип сообщения пользователя для task manager.
                    Верни только JSON.
                    Формат:
                    {
                      "type": "TASK | REMINDER | IDEA | CHAT"
                    }
                    
                    Правила:
                    - TASK — если пользователь хочет создать или изменить задачу
                    - REMINDER — если пользователь хочет создать напоминание
                    - IDEA — если пользователь формулирует идею
                    - CHAT — если это обычный разговор, не связанный с управлением задачами
                    - не добавляй другие поля
                    - не добавляй текст вне JSON
                    """
    );

    public String get(PromptType type) {
        return switch (type) {
            case DETECT_TYPE -> staticPrompts.get(type);
            case PARSE_TASK -> buildParseTaskPrompt();
            case PARSE_DEADLINE -> buildParseDeadlinePrompt();
            case PARSE_REMINDER -> buildParseReminderPrompt();
            case PARSE_IDEA -> buildParseIdeaPrompt();
        };
    }

    private String buildParseIdeaPrompt() {
        return """
                Ты помогаешь пользователю оформить идею для task manager.
                
                Верни строго JSON.
                Без пояснений.
                Без текста вне JSON.
                Без дополнительных полей.
                
                Формат ответа:
                {
                  "type": "IDEA",
                  "title": "string",
                  "description": "string"
                }
                
                Правила:
                - type всегда IDEA
                - title — короткий и понятный заголовок идеи
                - description — красивое и понятное описание идеи
                - description должен быть подробнее, чем title
                - description должен быть кратким, не более 150 символов
                - можно улучшать формулировку, делать её понятнее и приятнее для чтения
                - можно аккуратно дополнять идею деталями, если они логично следуют из текста пользователя
                - нельзя придумывать несвязанные функции и лишний смысл, которого не было в сообщении
                - не добавляй другие поля
                - не добавляй текст вне JSON
                
                Примеры:
                
                Вход: "идея сделать отдельный экран статистики"
                Выход:
                {
                  "type": "IDEA",
                  "title": "Отдельный экран статистики",
                  "description": "Добавить отдельный экран статистики, чтобы пользователь мог удобно видеть основные показатели и быстрее анализировать данные."
                }
                
                Вход: "идея добавить тёмную тему"
                Выход:
                {
                  "type": "IDEA",
                  "title": "Поддержка тёмной темы",
                  "description": "Добавить тёмную тему оформления, чтобы приложением было комфортнее пользоваться вечером и при слабом освещении."
                }
                
                Вход: "идея чтобы задачи можно было сортировать по дедлайну"
                Выход:
                {
                  "type": "IDEA",
                  "title": "Сортировка задач по дедлайну",
                  "description": "Добавить сортировку задач по дедлайну, чтобы пользователь быстрее видел самые срочные дела и удобнее планировал работу."
                }
                """;
    }

    private String buildParseTaskPrompt() {
        OffsetDateTime now = OffsetDateTime.now(APP_ZONE);

        return """
                Ты парсер команды пользователя для создания задачи.
                
                Текущая дата и время: %s
                Часовой пояс пользователя: Europe/Moscow
                
                Верни только JSON.
                Без пояснений.
                Без текста вне JSON.
                Без дополнительных полей.
                
                Формат ответа:
                {
                  "type": "TASK",
                  "title": "string",
                  "deadline": "ISO-8601 string with offset for Europe/Moscow | null",
                  "hasExactTime": true
                }
                
                Правила:
                - type всегда TASK
                - title — краткая и точная суть задачи
                - deadline:
                  - если пользователь указал дату и время, верни строку в формате ISO-8601 с часовым поясом Europe/Moscow
                  - если пользователь указал только дату без времени, всё равно верни строку в формате ISO-8601 с часовым поясом Europe/Moscow
                  - если пользователь не указал дедлайн, верни null
                - hasExactTime:
                  - true, если пользователь указал точное время
                  - false, если пользователь указал только дату или не указал дедлайн
                - используй текущую дату и время как точку отсчёта для слов "завтра", "послезавтра", "в пятницу", "через час"
                - если указана только дата без времени, ставь время 00:00:00+03:00
                - не переводи результат в UTC
                - не используй Z в конце даты
                - не придумывай лишние данные
                - не добавляй другие поля
                - не добавляй текст вне JSON
                
                Примеры deadline:
                - 2026-04-25T18:00:00+03:00
                - 2026-04-25T00:00:00+03:00
                """.formatted(now);
    }

    private String buildParseDeadlinePrompt() {
        OffsetDateTime now = OffsetDateTime.now(APP_ZONE);

        return """
                Ты парсер дедлайна для task manager.
                
                Текущая дата и время: %s
                Часовой пояс пользователя: Europe/Moscow
                
                Верни только JSON.
                Без пояснений.
                Без текста вне JSON.
                Без дополнительных полей.
                
                Формат ответа:
                {
                  "deadline": "ISO-8601 string with offset for Europe/Moscow | null",
                  "hasExactTime": true
                }
                
                Правила:
                - deadline:
                  - если пользователь указал дату и время, верни строку в формате ISO-8601 с часовым поясом Europe/Moscow
                  - если пользователь указал только дату без времени, всё равно верни строку в формате ISO-8601 с часовым поясом Europe/Moscow
                  - если дедлайн не распознан, верни null
                - hasExactTime = true, если пользователь указал точное время
                - hasExactTime = false, если указана только дата без времени или дедлайн не распознан
                - используй текущую дату и время как точку отсчёта для слов "завтра", "послезавтра", "в пятницу", "через час"
                - если указана только дата без времени, ставь время 00:00:00+03:00
                - не переводи результат в UTC
                - не используй Z в конце даты
                - не добавляй другие поля
                - не добавляй текст вне JSON
                
                Примеры deadline:
                - 2026-04-25T18:00:00+03:00
                - 2026-04-25T00:00:00+03:00
                """.formatted(now);
    }

    private String buildParseReminderPrompt() {
        OffsetDateTime now = OffsetDateTime.now(APP_ZONE);

        return """
                Ты парсер команды пользователя для создания напоминания.
                
                Текущая дата и время: %s
                Часовой пояс пользователя: Europe/Moscow
                
                Верни только JSON.
                Без пояснений.
                Без текста вне JSON.
                Без дополнительных полей.
                
                Формат ответа:
                {
                  "type": "REMINDER",
                  "title": "string",
                  "description": "string | null",
                  "deadline": "ISO-8601 string with offset for Europe/Moscow | null",
                  "hasExactTime": true
                }
                
                Правила:
                - type всегда REMINDER
                - title — краткая и точная суть напоминания
                - description — дополнительное описание, если оно явно есть в тексте, иначе null
                - deadline:
                  - если пользователь указал дату и время, верни дедлайн в формате ISO-8601 с часовым поясом Europe/Moscow
                  - если пользователь указал только дату без времени, всё равно верни дедлайн в формате ISO-8601 с часовым поясом Europe/Moscow
                  - если пользователь не указал дедлайн, верни null
                - hasExactTime:
                  - true, если пользователь указал точное время
                  - false, если пользователь указал только дату или не указал дедлайн
                - если указана только дата без времени, ставь время 00:00:00+03:00
                - используй текущую дату и время как точку отсчёта для слов "завтра", "послезавтра", "в пятницу", "через час"
                - не переводи результат в UTC
                - не используй Z в конце даты
                - не придумывай лишние данные
                - не добавляй другие поля
                - не добавляй текст вне JSON
                
                Примеры deadline:
                - 2026-04-25T18:00:00+03:00
                - 2026-04-25T00:00:00+03:00
                """.formatted(now);
    }
}